query findImage($id: String!){
    componentRichImage(id: $id) {
        sys {
            id
        }
        image {
            title
            description
            width
            height
            url
        }
        fullWidth
    }
}
