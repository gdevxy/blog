query findImage($id: String!, $preview: Boolean = false){
    componentRichImage(id: $id, preview: $preview) {
        sys {
            id
        }
        image {
            title
            description
            width
            height
            url
        }
        fullWidth
    }
}
